<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- The provider exposes virtual documents only; this resource is declared
         to satisfy the DocumentsProvider contract and is not used for real files. -->
    <cache-path name="internal_cache" path="." />
</paths>
